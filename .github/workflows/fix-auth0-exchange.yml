name: Fix Auth0 token exchange

on:
  workflow_dispatch:
  push:
    paths:
      - '.auth0-patch-trigger'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Patch Auth0 token exchange
        shell: bash
        run: |
          python3 -c 'from pathlib import Path
          p = Path("src/CareLink/Helpers/LoginHelpers/CareLinkService.vb")
          s = p.read_text(encoding="utf-8-sig")
          if "client_secret" in s and "CareLink/6.0.1.6" in s:
              print("Auth0 exchange is already patched.")
              raise SystemExit(0)
          a = """        Dim clientId As String = client.ClientId
        Dim scope As String = client.Scope
"""
          b = """        Dim clientId As String = client.ClientId
        Dim clientSecret As String = client.ClientSecret
        Dim scope As String = client.Scope
"""
          if a not in s: raise SystemExit("Could not find Auth0 client configuration block.")
          s = s.replace(a, b, 1)
          a = """        Dim form As New List(Of KeyValuePair(Of String, String)) From {
            New KeyValuePair(Of String, String)(key:="grant_type", value:="authorization_code"),
            New KeyValuePair(Of String, String)(key:="client_id", value:=clientId),
            New KeyValuePair(Of String, String)(key:="code", value:=redirectResult.Code),
            New KeyValuePair(Of String, String)(key:="redirect_uri", value:=redirectUri)}

        Dim content As New FormUrlEncodedContent(nameValueCollection:=form)
        Dim response As HttpResponseMessage = Await s_http.PostAsync(requestUri:=tokenUrl, content)
"""
          b = """        Dim form As New List(Of KeyValuePair(Of String, String)) From {
            New KeyValuePair(Of String, String)(key:="grant_type", value:="authorization_code"),
            New KeyValuePair(Of String, String)(key:="client_id", value:=clientId),
            New KeyValuePair(Of String, String)(key:="code", value:=redirectResult.Code),
            New KeyValuePair(Of String, String)(key:="redirect_uri", value:=redirectUri)}

        If Not String.IsNullOrWhiteSpace(clientSecret) Then
            form.Add(New KeyValuePair(Of String, String)(key:="client_secret", value:=clientSecret))
        End If

        Using content As New FormUrlEncodedContent(nameValueCollection:=form)
            Using request As New HttpRequestMessage(method:=HttpMethod.Post, requestUri:=tokenUrl)
                request.Headers.Accept.Add(New Headers.MediaTypeWithQualityHeaderValue("application/json"))
                request.Headers.UserAgent.ParseAdd("CareLink/6.0.1.6")
                request.Content = content
                Dim response As HttpResponseMessage = Await s_http.SendAsync(request)
"""
          if a not in s: raise SystemExit("Could not find Auth0 token request block.")
          s = s.replace(a, b, 1)
          a = """        If Not response.IsSuccessStatusCode Then
            message = $"Could not get token data in {NameOf(DoLoginAuth0Async)}: {body}"
            Throw New Exception(message)
        End If

        Dim token As TokenData = Nothing
"""
          b = """                If Not response.IsSuccessStatusCode Then
                    message = $"Could not get token data in {NameOf(DoLoginAuth0Async)}: {body}"
                    Throw New Exception(message)
                End If

                Dim token As TokenData = Nothing
"""
          if a not in s: raise SystemExit("Could not find Auth0 response block.")
          s = s.replace(a, b, 1)
          a = """        If Not body.TryFromJson(result:=token) Then
            message = "Failed to parse token response JSON."
            Throw New ApplicationException(message)
        End If
        token.ClientId = clientId
        WriteTokenFile(token, path:=outputFile)
        Return token
    End Function
"""
          b = """                If Not body.TryFromJson(result:=token) Then
                    message = "Failed to parse token response JSON."
                    Throw New ApplicationException(message)
                End If
                token.ClientId = clientId
                WriteTokenFile(token, path:=outputFile)
                Return token
            End Using
        End Using
    End Function
"""
          if a not in s: raise SystemExit("Could not find Auth0 method ending.")
          s = s.replace(a, b, 1)
          p.write_text(s, encoding="utf-8-sig", newline="\n")
          print("Auth0 token exchange patched successfully.")'

      - name: Verify patch
        shell: bash
        run: |
          grep -n -A8 -B4 'client_secret' src/CareLink/Helpers/LoginHelpers/CareLinkService.vb
          grep -n 'CareLink/6.0.1.6' src/CareLink/Helpers/LoginHelpers/CareLinkService.vb

      - name: Commit patched source
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/CareLink/Helpers/LoginHelpers/CareLinkService.vb
          if git diff --cached --quiet; then
            echo "No source changes to commit."
            exit 0
          fi
          git commit -m "Fix Auth0 token exchange request"
          git push
